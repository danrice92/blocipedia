<%= f.label :collaborators %>
<%= f.text_field :collaborators,
        class: 'form-control',
        placeholder: "Enter collaborator emails (separated by commas)",
        value: collaborators.map(&:email).join(', ') %>